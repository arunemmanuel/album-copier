# Contributing

## Development

```bash
source .venv/bin/activate
pip install -r requirements.txt
pytest
```

Keep business logic outside the UI where possible. Add tests for copy rules, reports, settings, and error handling when changing behavior.

## Pull Requests

- Include a short summary.
- Include test results.
- Update documentation for user-visible changes.
